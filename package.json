{
  "name": "angular-filterbox",
  "private": true,
  "version": "0.0.1",
  "description": "Filter box demo",
  "repository": "https://github.com/yikliu/angular-filterbox",
  "license": "MIT",
  "devDependencies": {
    "express": "^4.1.1",
    "morgan": "^1.0.0",
    "body-parser": "^1.0.2",
    "method-override": "^1.0.0",
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "karma": "~0.12",
    "karma-chrome-launcher": "^0.1.12",
    "karma-firefox-launcher": "^0.1.6",
    "karma-jasmine": "^0.3.5",
    "karma-junit-reporter": "^0.2.2",
    "protractor": "^2.1.0",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",

    "pretest": "npm install",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js  --single-run",
  }
}
